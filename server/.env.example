# Server Configuration
PORT=3001
NODE_ENV=development

# PostgreSQL Database Configuration
# Option 1: Use DATABASE_URL (recommended)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/lightning_payments

# Option 2: Or use individual variables (fallback if DATABASE_URL not set)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=lightning_payments
# DB_USER=postgres
# DB_PASSWORD=postgres

# Lightning Network Node A (Receiver Node - Alice)
# This node will generate invoices and receive payments
# Get these values from Polar after creating your network
LND_A_HOST=localhost:10003
LND_A_CERT_PATH=/path/to/.polar/networks/1/volumes/lnd/alice/tls.cert
LND_A_MACAROON_PATH=/path/to/.polar/networks/1/volumes/lnd/alice/data/chain/bitcoin/regtest/admin.macaroon

# Lightning Network Node B (Sender Node - Bob)
# This node will pay invoices
# Get these values from Polar after creating your network
LND_B_HOST=localhost:10004
LND_B_CERT_PATH=/path/to/.polar/networks/1/volumes/lnd/bob/tls.cert
LND_B_MACAROON_PATH=/path/to/.polar/networks/1/volumes/lnd/bob/data/chain/bitcoin/regtest/admin.macaroon

# CORS Configuration
# Allow requests from the Next.js frontend
CORS_ORIGIN=http://localhost:3000
